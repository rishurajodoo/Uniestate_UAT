<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--Loan Tree view-->
    <record id="hr_loan_view_tree" model="ir.ui.view">
        <field name="name">hr.loan.view.tree</field>
        <field name="model">hr.loan</field>
        <field name="arch" type="xml">
            <tree string="Loan Requests">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="loan_amount"/>
                <field name="date"/>
                <field name="principal_product_id"/>
                <field name="interest_product_id"/>
            </tree>
        </field>
    </record>
    <!--Loan Form view-->
    <record id="hr_loan_view_form" model="ir.ui.view">
        <field name="name">hr.loan.view.form</field>
        <field name="model">hr.loan</field>
        <field name="arch" type="xml">
            <form string="Loan Request">
                <header>
                    <button name="action_compute_installment" type="object" string="Compute Installment" class="oe_highlight"  invisible="state in ('approve', 'refuse', 'waiting_approval_1')"/>
                     <button name="action_running" type="object" string="Confirm" invisible="state != 'draft'" class="oe_highlight"/>
                    <button name="action_create_bill" type="object" string="Create Bill" invisible="state != 'running'" class="oe_highlight"/>
                    <button name="action_submit" type="object" string="Submit" invisible="state != 'running'" class="oe_highlight"/>
                    <button name="action_cancel" type="object" string="Cancel" invisible="state != 'draft'"/>
<!--                    <button name="action_refuse" type="object" string="Refuse" invisible="state != ('draft','waiting_approval_1')" class="oe_highlight" groups="hr.group_hr_manager"/>-->
                    <field name="state" widget="statusbar" statusbar_visible="draft,'running',waiting_approval_1,approve" />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button class="oe_stat_button" name="action_get_bill_invoice_jv" type="object" icon="fa-bars"
                                invisible="invoice_count == 0"
                                >
                            <field name="invoice_count" widget="statinfo" string="Invoices"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group col="4">
                        <field name="partner_id" readonly="state == 'approve'"/>
                        <field name="date"/>
<!--                        <field name="department_id"/>-->
<!--                        <field name="job_position_id"/>-->
                        <field name="loan_amount" readonly="state == 'approve'"/>
                        <field name="installment" readonly="state == 'approve'"/>
                        <field name="payment_date" readonly="state == 'approve'"/>
                        <field name="company_id"
                               options="{'no_create': True}"
                               readonly="state != 'draft'"
                               groups="base.group_multi_company"/>
                        <field name="currency_id" options="{'no_create': True}" groups="base.group_multi_company"/>
                        <field name="principal_product_id"/>
                        <field name="interest_product_id"/>
                        <field name="account_id"/>
                    </group>
                    <notebook>
                        <page string="Installments">
                            <field name="loan_line_ids">
                                <tree string="Installments" editable="bottom" decoration-info="paid == True">
                                    <field name="date"/>
                                    <field name="amount"/>
                                    <field name="interest_amount"/>
                                    <field name="paid" column_invisible="True"/>
                                    <field name="int_pa_amount" string="Total Amount"/>
                                </tree>
                            </field>
                            <group class="oe_subtotal_footer oe_right" colspan="2" >
                                <field name="total_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                <field name="total_interest_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                <field name="total_principal_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                <field name="total_paid_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                <field name="balance_amount" class="oe_subtotal_footer_separator" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                            </group>
                            <div class="oe_clear"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>
    <!--loan search view-->
    <record id="hr_loan_view_search" model="ir.ui.view">
        <field name="name">hr.loan.view.search</field>
        <field name="model">hr.loan</field>
        <field name="arch" type="xml">
           <search string="Loan">
               <filter string="My Requests" name="myrequest" domain="[('partner_id.user_id.id', '=', uid)]" />
                <field name="partner_id" string="Partner"/>
<!--                <field name="department_id" string="Department"/>-->
                    <filter string="Partner" name="partner_id" context="{'group_by':'partner_id'}"/>
<!--                    <filter string="Department" name="department_id" context="{'group_by':'department_id'}"/>-->
                    <filter string="Status" name="status" context="{'group_by':'state'}"/>
            </search>
        </field>
    </record>
<!--  Record action for hr loan.  -->
    <record id="hr_loan_action" model="ir.actions.act_window">
        <field name="name">Request for Loan</field>
        <field name="res_model">hr.loan</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_myrequest': 1}</field>
        <field name="search_view_id" ref="hr_loan_view_search"/>
         <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click to create a new Loan request.
            </p><p>
                Use this menu to create loan requests.
            </p>
        </field>
    </record>
    <!-- Menu records for managing menus -->
    <menuitem
            id="requisition_loan_menu_for_loan_and_advances"
            name="Loans &amp; Advances"
            parent="material_purchase_requisitions.menu_purchase_requisition"
            sequence="20"/>
    <menuitem
            id="requisition_loan_menu_for_loan"
            name="Loan"
            parent = "requisition_loan_menu_for_loan_and_advances"
            sequence="1"/>
    <menuitem
            id="requisition_loan_menu_creates_request_for_loan"
            name="Request for Loan"
            parent="requisition_loan_menu_for_loan"
            action="hr_loan_action"/>
</odoo>
