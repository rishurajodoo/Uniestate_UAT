<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--    Inheriting account.payment.term for adding interest related fields -->
    <record id="view_payment_term_form" model="ir.ui.view">
        <field name="name">account.payment.term.inherit.account.interest</field>
        <field name="model">account.payment.term</field>
        <field name="inherit_id" ref="account.view_payment_term_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='company_id']" position="after">
                <field name="interest_overdue_act"/>
                <field name="interest_type"
                       required="interest_overdue_act" invisible="not interest_overdue_act"/>
<!--                <field name="interest_percentage"-->
<!--                       required="interest_overdue_act" invisible="not interest_overdue_act"/>-->
                <field name="interest_product_ids" widget="many2many_tags"
                       invisible="not interest_overdue_act"/>
            </xpath>
            <xpath expr="//group[2]" position="before">
                <group string="Interest Overdue Invoice" invisible="not interest_overdue_act">
                    <field name="account_payment_interest_ids" widget="payment_term_line_ids" nolabel="1" colspan="2">
                        <tree string="Payment Interest" editable="bottom" no_open="True">
                            <field name="sequence"/>
                            <field name="interest_payment_type" string="Type"/>
                            <field name="interest_duration" string="Interest Duration"/>
                            <field name="amount_percentage"/>
                            <field name="start_day"/>
                            <field name="end_day"/>
                            <field name="account_payment_term_id" column_invisible="True"/>
                        </tree>
                    </field>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
